const Url = require('../models');
const shortid = require('short-unique-id');

exports.shortenUrl = async (req, res) => {
  const { originalUrl, user, customSlug } = req.body;
  try {
    // Check if the customSlug
  } catch (err) {
    
  }
}